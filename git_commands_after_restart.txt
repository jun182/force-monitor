🌸 Force Monitor - Post-Install Git Commands 🌸
==================================================

After restarting PowerShell/VS Code, run these commands:

1. Navigate to project directory:
   cd "c:\Users\johnny.hamnesjo\Desktop\openscale-project"

2. Initialize git repository:
   git init

3. Set your git identity (replace with your info):
   git config user.name "Johnny Hamnesjö Olausson"
   git config user.email "johnny.hamnesjo@chalmers.se"

4. Add all files:
   git add .

5. Make first commit:
   git commit -m "🌸 Initial commit: Force Monitor v2.0 - Kawaii Edition

   - Professional weight measurement interface for SparkFun OpenScale
   - Beautiful kawaii-styled GUI and terminal applications
   - Session statistics with calibration and CSV export
   - GPL v3.0 licensed
   - Contact: johnny.hamnesjo@chalmers.se"

6. Create GitHub repository at: https://github.com/new
   - Repository name: force-monitor
   - Description: 🌸 Kawaii-styled precision weight measurement interface for SparkFun OpenScale
   - Make it public
   - Don't initialize with README (we already have one)

7. Add remote and push:
   git remote add origin https://github.com/YOUR_USERNAME/force-monitor.git
   git branch -M main
   git push -u origin main

🌸 Ready to upload when you restart PowerShell! 🌸
